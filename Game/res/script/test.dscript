

set dir;

set r = 60;
set c = 2048;

while(true){
    for(i in c){
        bullet(dir + i*(360/c));
    }
    break;
    dir += 2;
    
    wait;
}

task bullet(dir){
    set b = bullet(_star, _purple, 320, 240, dir, 0.1);
}