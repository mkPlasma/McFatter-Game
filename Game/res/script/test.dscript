
set const c = 6;

set dir;
set dirAdd;

while(true){
	for(d < 0, 360/c, 360)
		split({_cx, _top + 120}, dir + d, 0);
	
	dir += dirAdd;
	dirAdd -= 0.1;
	
	wait 1;
}

task split(pos, dir, iterations){
	
	set b = bullet(_orb, _red_d, _cx, _cy, dir, 2, 15);
	
	wait 30;
	
	
}