
set ang = 20;

while(true){
    
    for(i in -2, 3){
        bulletStack(getAngleToPlayer({400, 200}) + i*ang);
    }
    
    wait 20;
}

function bulletStack(dir){
    for(i in 5){
        bullet(_scale_d, _dark_yellow, 400, 200, dir, 2 + i/2);
    }
}