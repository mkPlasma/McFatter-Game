
set c = 30;

while(true){
    
    for(i in c){
        bulletStack(getAngleToPlayer({400, 200}) + i*(360/c));
    }
    
    wait 10;
}

function bulletStack(dir){
    for(i in 10){
        bullet(_crystal, _red, 400, 200, dir, 2 + i/2);
    }
}