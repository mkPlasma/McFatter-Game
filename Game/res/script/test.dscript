

set dir;

set r = 60;
set c = 8;

while(true){
    
    for(i in c){
        bullet(dir + i*(360/c), i);
    }
    
    dir += 5;
    
    wait;
}

task bullet(dir, color){
    set b = bullet(_scale, _red, centerX(), 140, dir, 5, 10);
    //b.setAdditive();
}