
set const c = 6;

set dir;
set dirAdd;

while(true){
	for(d < 0, 360/c, 360)
		split({_cx, _top + 120}, dir + d);
	
	dir += dirAdd;
	dirAdd -= 0.1;
	
	wait 1;
}

task split(pos, dir){
	
	set b = bullet(_orb, _red, pos, dir, 2, 15);
	
}