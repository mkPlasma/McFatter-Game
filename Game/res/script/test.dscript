

set dir;

set r = 60;
set c = 8;

while(true){
    
    //dir = random(0, 360);
    
    for(i in c){
        bullet(dir + i*(360/c));
    }
    
    dir += 0.05;
    
    wait;
}

task bullet(dir){
    set b = bullet(_laser_helix, _red_d, 320, 120, dir, 16);
}